{
  "environments": {
    "review": {
      "addons": ["heroku-postgresql:hobby-dev"],
      "scripts": {
        "postdeploy": "npm run dbmigrate up && psql -f 'db/seeds.sql' $DATABASE_URL"
      }
    }
  }
}
